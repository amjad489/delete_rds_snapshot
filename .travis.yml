---
env:
  - distro: ubuntu1604
  - distro: ubuntu1404

language: python
python: "2.7"

before_install:
  - sudo apt-get update -qq
  - sudo apt-get install -qq python-apt
sudo: enabled
script:
  - python delete_rds_snapshot.py
